{
  "name": "metaref",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "deploy": "sls deploy",
    "start": "ts-node src/index.ts",
    "postbuild": "copy package.json dist\\package.json && copy package-lock.json dist\\package-lock.json && cd dist && npm ci --only=production",
    "set-webhook": "telegraf -m setWebhook"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "dotenv": "^16.4.4",
    "@aws-sdk/client-dynamodb": "^3.513.0",
    "serverless-http": "^3.2.0",
    "telegraf": "^4.15.3",
    "ts-node": "^10.9.2"
  }
}
